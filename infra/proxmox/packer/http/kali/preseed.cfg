# TrueNorth Range — Kali Linux Preseed Configuration
# Debian-installer preseed for automated Kali installation

# Locale and keyboard
d-i debian-installer/locale string en_US.UTF-8
d-i keyboard-configuration/xkb-keymap select us
d-i console-keymaps-at/keymap select us

# Network
d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string kali
d-i netcfg/get_domain string range.local
d-i netcfg/hostname string kali

# Mirror
d-i mirror/country string enter information manually
d-i mirror/http/hostname string http.kali.org
d-i mirror/http/directory string /kali
d-i mirror/http/proxy string

# Clock and timezone
d-i clock-setup/utc boolean true
d-i time/zone string UTC
d-i clock-setup/ntp boolean true

# Partitioning — single ext4 partition, entire disk
d-i partman-auto/method string regular
d-i partman-auto/choose_recipe select atomic
d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true
d-i partman-lvm/confirm boolean true
d-i partman-lvm/confirm_nooverwrite boolean true

# Root account disabled; use kali user
d-i passwd/root-login boolean false

# Default kali user
d-i passwd/user-fullname string Kali User
d-i passwd/username string kali
d-i passwd/user-password password kali
d-i passwd/user-password-again password kali
d-i user-setup/allow-password-weak boolean true
d-i user-setup/encrypt-home boolean false

# Package selection
tasksel tasksel/first multiselect standard
d-i pkgsel/include string openssh-server qemu-guest-agent sudo curl wget git net-tools
d-i pkgsel/upgrade select full-upgrade

# Enable SSH
d-i preseed/late_command string \
  in-target systemctl enable ssh ; \
  in-target systemctl enable qemu-guest-agent ; \
  echo "kali ALL=(ALL) NOPASSWD:ALL" > /target/etc/sudoers.d/kali ; \
  chmod 0440 /target/etc/sudoers.d/kali ; \
  in-target sed -i 's/^#PermitRootLogin.*/PermitRootLogin no/' /etc/ssh/sshd_config ; \
  in-target sed -i 's/^#PasswordAuthentication.*/PasswordAuthentication yes/' /etc/ssh/sshd_config

# GRUB bootloader
d-i grub-installer/only_debian boolean true
d-i grub-installer/bootdev string default

# Avoid the last prompt about install being complete
d-i finish-install/reboot_in_progress note

# Power off after install so Packer can detect completion
d-i debian-installer/exit/poweroff boolean true