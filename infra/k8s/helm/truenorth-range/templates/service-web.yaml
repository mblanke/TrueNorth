{{- if .Values.web.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "truenorth-range.fullname" . }}-web
  labels:
    {{- include "truenorth-range.componentLabels" (dict "context" . "component" "web") | nindent 4 }}
spec:
  type: {{ .Values.web.service.type }}
  ports:
    - port: {{ .Values.web.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "truenorth-range.componentSelectorLabels" (dict "context" . "component" "web") | nindent 4 }}
{{- end }}