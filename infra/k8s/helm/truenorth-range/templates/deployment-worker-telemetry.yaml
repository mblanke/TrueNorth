{{- if .Values.workerTelemetry.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "truenorth-range.fullname" . }}-worker-telemetry
  labels:
    {{- include "truenorth-range.componentLabels" (dict "context" . "component" "worker-telemetry") | nindent 4 }}
spec:
  {{- if not (.Values.workerTelemetry.autoscaling).enabled }}
  replicas: {{ .Values.workerTelemetry.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "truenorth-range.componentSelectorLabels" (dict "context" . "component" "worker-telemetry") | nindent 6 }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        checksum/secret: {{ include (print $.Template.BasePath "/secret.yaml") . | sha256sum }}
        {{- if .Values.monitoring.prometheus.enabled }}
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
        prometheus.io/path: {{ .Values.monitoring.prometheus.path | default "/metrics" }}
        {{- end }}
      labels:
        {{- include "truenorth-range.componentSelectorLabels" (dict "context" . "component" "worker-telemetry") | nindent 8 }}
    spec:
      serviceAccountName: {{ include "truenorth-range.serviceAccountName" . }}
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.workerTelemetry.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.workerTelemetry.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.workerTelemetry.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: worker-telemetry
          image: {{ include "truenorth-range.image" (dict "image" .Values.workerTelemetry.image "global" .Values.global "appVersion" .Chart.AppVersion) }}
          imagePullPolicy: {{ .Values.workerTelemetry.image.pullPolicy }}
          command:
            - celery
            - -A
            - worker.celery_app
            - worker
            - -Q
            - {{ .Values.workerTelemetry.queues | quote }}
            - -c
            - {{ .Values.workerTelemetry.concurrency | quote }}
            - --loglevel=info
            - -n
            - "telemetry@%h"
          envFrom:
            - configMapRef:
                name: {{ include "truenorth-range.fullname" . }}-config
            - secretRef:
                name: {{ include "truenorth-range.fullname" . }}-secret
          env:
            - name: DATABASE_URL
              value: {{ include "truenorth-range.databaseUrl" . | quote }}
            - name: REDIS_URL
              value: {{ include "truenorth-range.redisUrl" . | quote }}
            {{- with .Values.workerTelemetry.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - celery -A worker.celery_app inspect ping -d celery@$HOSTNAME
            initialDelaySeconds: 30
            periodSeconds: 60
            timeoutSeconds: 10
            failureThreshold: 3
          resources:
            {{- toYaml .Values.workerTelemetry.resources | nindent 12 }}
{{- end }}