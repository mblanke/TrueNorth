---
# OpenSearch index templates, ISM policies and ingest pipelines.
#
# Re-runnable on its own:  ansible-playbook playbooks/70-telemetry.yml
- name: "OpenSearch index templates, ISM policies and ingest pipelines."
  hosts: platform
  gather_facts: true
  roles:
    - role: tn_telemetry
      tags: [telemetry]
