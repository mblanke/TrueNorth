---
# Preflight — verify the platform host is ready. Read-only; nothing is changed.
#
# Re-runnable on its own:  ansible-playbook playbooks/00-preflight.yml
- name: "Preflight"
  hosts: platform
  gather_facts: true
  roles:
    - role: tn_preflight
      tags: [preflight]
