---
# vSphere provider wiring and the provisioning-privilege check.
#
# Re-runnable on its own:  ansible-playbook playbooks/90-vsphere.yml
- name: "vSphere provider wiring and the provisioning-privilege check."
  hosts: platform
  gather_facts: true
  roles:
    - role: tn_vsphere
      tags: [vsphere]
