---
# Realm, AD federation and the token claim contract registration depends on.
#
# Re-runnable on its own:  ansible-playbook playbooks/60-keycloak.yml
- name: "Realm, AD federation and the token claim contract registration depends on."
  hosts: platform
  gather_facts: true
  roles:
    - role: tn_keycloak
      tags: [keycloak]
