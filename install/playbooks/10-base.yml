---
# Host preparation — packages, sysctl, data directory tree.
#
# Re-runnable on its own:  ansible-playbook playbooks/10-base.yml
- name: "Host preparation"
  hosts: platform
  gather_facts: true
  roles:
    - role: tn_base
      tags: [base]
