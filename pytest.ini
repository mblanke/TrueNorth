[pytest]
testpaths = tests
pythonpath =
    control-plane/api
    control-plane/worker
    scenario-engine
    ai-orchestrator
    tools
addopts = -v --tb=short
markers =
    slow: marks tests as slow
    integration: marks tests requiring external services