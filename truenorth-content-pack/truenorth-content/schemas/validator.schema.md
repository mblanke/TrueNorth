# Validator conventions
Each validator = one markdown file: a fenced OpenSearch query + a pass rule.
- crit_*        : must-pass; maps to ONE QSP critical event; returns pass/fail.
- deliverable_* : report submitted + follows template (format/completeness).
- manual_ack_*  : NO auto score; Standards checklist for analysis-quality judgement.
